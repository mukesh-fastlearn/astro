# Dependencies
node_modules/

# Next.js build output
.next/
out/
*.tsbuildinfo

# Secrets — never commit
*.pem
*.key
.env
.env.*
!.env.example
api.txt

# Third-party clone (Scrapegraph-ai has its own git repo)
scraper/Scrapegraph-ai/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Vertex AI gateway key - never commit
VERTEX-GATEWAY-CREDENTIALS.txt
*CREDENTIALS*.txt
credentials.json

# RAG index is 3.6MB of build output - regenerate with scripts/build-rag-index.ts
server/rag-index.json
server/.rag-checkpoint.json

# SQLite database — never commit user data
server/data/
*.db
*.db-shm
*.db-wal
